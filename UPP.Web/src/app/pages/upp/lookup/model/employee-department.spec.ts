import { EmployeeDepartment } from './employee-department';

describe('EmployeeDepartment', () => {
  it('should create an instance', () => {
    expect(new EmployeeDepartment()).toBeTruthy();
  });
});
