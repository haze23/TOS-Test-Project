import { BookingDescription } from './booking-description';

describe('BookingDescription', () => {
  it('should create an instance', () => {
    expect(new BookingDescription()).toBeTruthy();
  });
});
